

let cheroso = "Diogo"; // o let é usado somente no bloco atual da função, famosa variavel local
var diogo = "Cheroso";// Var é global, usado para o código tudo e não só no bloco atual;
const MAE_DO_DIOGO = "kelly"; // const é algo que não muda, é algo constante.
const idade = 17;
const LINES = "=============================================="
console.log(`${LINES}\n`);
let msg = `Meu amigo ${cheroso}, vulgo ${diogo} é filho da ${MAE_DO_DIOGO},\nele tem ${idade} anos de idade.\n`;
console.log(msg); 
console.log(LINES);

// JS usa sensitive case, meio que padrão de todas outras linguagens.

